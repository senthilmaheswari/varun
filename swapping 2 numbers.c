#include <stdio.h>
void swap(int ,int)
void main()
{
int a=5,b=3;
printf("before swap in a&b %d%d",a b);
swap(a,b);
printf("after swap a&b are %d%d",a b);
}
void swap (int a,intb)
{
a=a+b;
b=a_b;
a=a_b;
printf("in swap function a&b value are %d%d",a ,b);
}


