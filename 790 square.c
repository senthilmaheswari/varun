#include <stdio.h>

int main(void) {
	int a,b,m,k,d=0;
	scanf("%d\n%d",&a,&b);
	m=a*b;
	for(k=1;k<c;k++)
	{
		if(m/k==i)
		{
			d++;
		}
	}
	if(d==0)
		{
		printf("it is not a perfect square");
		}
		else
		{
			printf("it is a perfect square");
		}
	
	return 0;
}
